import { styled } from '@mui/system';
import { Box } from '@mui/material';

export const Footer = styled('footer')({
  marginTop: '10rem',
  height: '70vh',
  backgroundColor: '#285ede',
  display: 'flex',
  alignItems: 'flex-end',
  padding: '10rem',
  color: '#FFFFFF',
  fontSize: '3rem',
  fontWeight: 600,
  background: '#CE897B',
});

export const Loader = styled(Box)({});
